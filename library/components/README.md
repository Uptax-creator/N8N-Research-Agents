# 🔧 COMPONENTS LIBRARY

## Structure:
```
library/components/
├── evaluation/          # Evaluation components
│   ├── results-processor.js
│   ├── test-validator.js
│   └── set-test-configurations.json
├── processors/          # Data processors
│   ├── csv-parser.js
│   └── config-loader.js
└── formatters/         # Response formatters
    └── response-formatter.js
```

## GitHub URLs:
```javascript
// Evaluation Components
const RESULTS_PROCESSOR = `${GITHUB_BASE}/library/components/evaluation/results-processor.js`;
const TEST_VALIDATOR = `${GITHUB_BASE}/library/components/evaluation/test-validator.js`;
const TEST_CONFIG = `${GITHUB_BASE}/library/components/evaluation/set-test-configurations.json`;
```